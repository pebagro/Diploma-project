@model EFinzynierka.Models.Login

<h2> Zaloguj się</h2>
<hr>
@using (Html.BeginForm("Login", "Account", FormMethod.Post))
{
    @Html.ValidationSummary()
    ;

    <form>
       @* <div class="form-group">
            <label for="exampleInputEmail1">Nazwa użytkownika</label>
            @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
        </div>*@


        <div class="form-group">
  <label for="exampleInputEmail1">Adres e-mail</label>
  @Html.TextBoxFor(x => x.Email, new { @class = "form-control", @id = "exampleInputEmail1", @aria_describedby = "emailHelp" })
</div>

<div class="form-group">
  <label for="exampleInputPassword1">Hasło</label>
  @Html.PasswordFor(x => x.Password, new { @class = "form-control", @id = "exampleInputPassword1" })
</div>
<br>
        <button type="submit" class="btn btn-primary">Zaloguj się</button>
    </form>

}